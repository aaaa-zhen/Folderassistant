name: Build & Package

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: windows-latest
            platform: win32
          - os: macos-latest
            platform: darwin

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Package app
        run: npx electron-forge make

      - name: Ad-hoc sign macOS app
        if: matrix.platform == 'darwin'
        run: |
          APP_PATH=$(find out -name "*.app" -type d | head -1)
          if [ -n "$APP_PATH" ]; then
            echo "Signing $APP_PATH"
            codesign --force --deep --sign - "$APP_PATH"
            echo "Signed successfully"
          fi

      - name: Re-zip signed macOS app
        if: matrix.platform == 'darwin'
        run: |
          ZIP_PATH=$(find out/make -name "*.zip" | head -1)
          APP_DIR=$(find out -name "*.app" -type d -path "*/darwin/*" | head -1)
          if [ -n "$ZIP_PATH" ] && [ -n "$APP_DIR" ]; then
            APP_PARENT=$(dirname "$APP_DIR")
            rm "$ZIP_PATH"
            cd "$APP_PARENT"
            zip -r -y "$OLDPWD/$ZIP_PATH" "$(basename "$APP_DIR")"
            echo "Re-zipped: $ZIP_PATH"
          fi

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: folder-assistant-${{ matrix.platform }}
          path: out/make/**/*
          if-no-files-found: warn
